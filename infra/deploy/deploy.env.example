# ============================================================
# AIFarm 배포 설정 파일
# ============================================================
# 이 파일을 deploy.env로 복사하고 값을 수정하세요
# 
# IMPORTANT: Input Format Guidelines
# ----------------------------------
# - Values should contain only safe characters
# - ALLOWED: alphanumeric, dots (.), hyphens (-), underscores (_), 
#            forward slashes (/), backslashes (\), colons (:)
# - NOT ALLOWED: & | < > ^ ` % " (shell special characters)
# - Do not wrap values in quotes unless they are part of the value
# - No spaces around the = sign
# - Lines starting with # are comments (ignored)
# ============================================================

# 서버 설정
# ---------
# Server IP address or hostname (e.g., 192.168.1.100 or myserver.example.com)
# Format: alphanumeric, dots, hyphens only
AIFARM_SERVER_HOST=your-server-ip

# Deploy user (lowercase letters, numbers, underscore, hyphen)
# IMPORTANT: Do NOT use root for production deployments
# Create a dedicated deploy user with sudo access instead
AIFARM_SERVER_USER=deploy

# SSH port (default: 22)
AIFARM_SERVER_PORT=22

# 로컬 경로 설정
# --------------
# Path to your local aifarm project directory
# Use forward slashes or escaped backslashes
# Example Windows: d:/exe.blue/ai-fram/aifarm
# Example Linux: /home/user/projects/aifarm
AIFARM_LOCAL_PATH=d:/exe.blue/ai-fram/aifarm

# SSH 키 경로 (선택사항)
# ---------------------
# Path to SSH private key for authentication
# Leave commented to use default SSH key (~/.ssh/id_rsa or ~/.ssh/id_ed25519)
# AIFARM_SSH_KEY=~/.ssh/id_ed25519_aifarm

# ============================================================
# Security Notes
# ============================================================
# 1. Never commit deploy.env to version control (it's in .gitignore)
# 2. Use SSH key authentication, not passwords
# 3. Create a dedicated deploy user instead of using root
# 4. Ensure the deploy user has sudo access for service management
# 5. Review and restrict permissions on the deploy.env file
# ============================================================