# AIFARM AutoX.js Scripts

YouTube 자동화를 위한 AutoX.js 스크립트 모음

## 📁 프로젝트 구조

```
autox-scripts/
├── main.js                 # 메인 엔트리 포인트 (폴링 루프)
├── modules/
│   ├── api.js             # Backend API 통신
│   ├── youtube.js         # YouTube 앱 자동화
│   ├── human.js           # 인간 패턴 시뮬레이션
│   └── logger.js          # 로깅 모듈
├── config/
│   ├── dev.json           # 개발 환경 설정
│   └── prod.json          # 프로덕션 환경 설정
├── tests/
│   └── simulator.js       # 로컬 테스트용 시뮬레이터
└── README.md
```

## 🚀 시작하기

### 1. AutoX.js 앱 설치

안드로이드 폰에 AutoX.js 앱을 설치합니다.

- [AutoX.js GitHub](https://github.com/kkevsekk1/AutoX)
- [AutoX.js Releases](https://github.com/kkevsekk1/AutoX/releases)

### 2. VS Code 확장 설치

```bash
# VS Code에서 확장 검색
Autox.js-VSCodeExt
```

### 3. 설정 파일 수정

`config/dev.json` 파일을 열어 서버 주소와 디바이스 ID를 설정합니다:

```json
{
  "server": {
    "host": "192.168.0.100",  // ← 여기를 PC IP로 변경
    "port": 8000,
    "protocol": "http"
  },
  "device": {
    "id": "PHONE_001",  // ← 고유한 디바이스 ID
    "model": "Xiaomi Redmi",
    "pc_id": "PC_01"
  }
}
```

### 4. 백엔드 서버 실행

```bash
cd backend
python main.py
# 서버가 http://0.0.0.0:8000 에서 실행됨
```

### 5. AutoX.js와 연결

#### 방법 1: 무선 연결 (Wi-Fi)
1. VS Code에서 `Ctrl+Shift+P` → `Autox.js: Start All Server`
2. 폰의 AutoX.js 앱에서 "연결" → PC의 IP 주소 입력
3. 또는 QR 코드 스캔

#### 방법 2: USB 연결 (ADB)
1. 폰 설정 → 개발자 옵션 → USB 디버깅 활성화
2. USB 케이블로 연결
3. VS Code에서 자동으로 디바이스 인식

### 6. 스크립트 실행

#### VS Code에서 실행
1. `main.js` 파일 열기
2. `F5` 누르기 또는 우측 상단 실행 버튼 클릭

#### AutoX.js 앱에서 직접 실행
1. 프로젝트 전체를 폰에 저장
2. AutoX.js 앱에서 `main.js` 선택
3. 재생 버튼 클릭

## 🔧 설정

### config/dev.json (개발 환경)

```json
{
  "server": {
    "host": "192.168.0.100",      // 로컬 네트워크 IP
    "port": 8000,
    "protocol": "http"
  },
  "settings": {
    "polling_interval": 3000,     // 3초마다 작업 확인
    "log_level": "info"           // 로그 레벨: debug, info, warn, error
  },
  "youtube": {
    "min_watch_time": 30,         // 최소 시청 시간 (초)
    "max_watch_time": 180,        // 최대 시청 시간 (초)
    "like_probability": 0.3,      // 좋아요 확률 (30%)
    "comment_probability": 0.1,   // 댓글 확률 (10%)
    "subscribe_probability": 0.05 // 구독 확률 (5%)
  }
}
```

### config/prod.json (프로덕션)

```json
{
  "server": {
    "host": "withb.io",           // 도메인
    "port": 8000,
    "protocol": "https"           // HTTPS 사용
  },
  "settings": {
    "polling_interval": 5000,     // 5초마다 작업 확인
    "log_level": "warn"           // 경고 이상만 로그
  }
}
```

## 📱 작동 원리

### 전체 플로우

```
┌─────────────┐       ┌──────────────┐       ┌─────────────┐
│   관리자    │       │   Backend    │       │  AutoX.js   │
│ (Frontend)  │       │   Server     │       │  (Phone)    │
└─────────────┘       └──────────────┘       └─────────────┘
      │                     │                       │
      │  1. 작업 등록       │                       │
      │──────────────────>│                       │
      │   POST /api/tasks  │                       │
      │                     │                       │
      │                     │  2. 작업 요청         │
      │                     │<──────────────────────│
      │                     │   GET /api/tasks/next │
      │                     │                       │
      │                     │  3. 작업 전달         │
      │                     │──────────────────────>│
      │                     │   { task_id, title }  │
      │                     │                       │
      │                     │                       │ 4. YouTube
      │                     │                       │    시청 수행
      │                     │                       │
      │                     │  5. 완료 보고         │
      │                     │<──────────────────────│
      │                     │   POST /complete      │
      │                     │                       │
      │  6. 통계 조회       │                       │
      │<──────────────────│                       │
      │   GET /stats        │                       │
```

### main.js 루프

```javascript
while (true) {
    // 1. 서버에서 작업 가져오기
    task = api.getNextTask();

    if (task) {
        // 2. YouTube 앱 실행
        youtube.launchYouTube();

        // 3. 영상 찾기 (URL 또는 키워드)
        youtube.searchByKeyword(task.keyword);

        // 4. 영상 시청
        youtube.watchVideo(task);

        // 5. 좋아요/댓글 (확률적)
        youtube.clickLike();
        youtube.writeComment();

        // 6. 구독/알림/공유/재생목록 (확률적)
        youtube.clickSubscribe();
        youtube.setNotification();  // 구독했을 경우
        youtube.shareVideo();       // 가끔
        youtube.addToPlaylist();    // 가끔

        // 7. 결과 보고
        api.completeTask(task.task_id, result);
    }

    // 7. 3초 대기 후 반복
    sleep(3000);
}
```

## 🧪 테스트

### 로컬 시뮬레이터 (PC에서 테스트)

```bash
node tests/simulator.js
```

이 시뮬레이터는:
- Backend API 호출 테스트
- 작업 흐름 확인
- 실제 폰 없이 로직 검증

## 🐛 디버깅

### VS Code 개발자 도구에서 로그 확인

1. `Ctrl+Shift+P` → `Toggle Developer Tools`
2. `Console` 탭에서 실시간 로그 확인

### 로그 레벨 설정

```json
{
  "settings": {
    "log_level": "debug"  // debug, info, warn, error
  }
}
```

## ✨ YouTube 자동화 기능

### 기본 기능
- ✅ **영상 시청**: 키워드 검색 또는 URL 직접 열기
- ✅ **시청 시간**: 30~180초 랜덤 (설정 가능)
- ✅ **자연스러운 행동**: 랜덤 터치, 스크롤 패턴

### 인터랙션 기능
- ✅ **좋아요**: 확률 기반 (기본 30%)
- ✅ **댓글 작성**: 확률 기반 (기본 10%)
- ✅ **구독**: 확률 기반 (기본 5%)
- ✅ **알림 설정**: 구독 시 전체 알림으로 자동 설정
- ✅ **공유**: 공유 메뉴 열기 (기본 5% 확률)
- ✅ **재생목록 추가**: "나중에 볼 동영상"에 추가 (기본 10% 확률)

### 추적 데이터
모든 작업 결과는 Backend에 기록됩니다:
```json
{
  "watch_duration": 142,
  "liked": true,
  "commented": false,
  "subscribed": true,
  "notification_set": true,
  "shared": false,
  "added_to_playlist": true
}
```

## 📊 통계 및 모니터링

### 실시간 로그 예시

```
[2025-01-15T10:30:00Z] [PHONE_001] [INFO] 작업 요청 중...
[2025-01-15T10:30:01Z] [PHONE_001] [INFO] 작업 수신 성공 {"task_id":123,"title":"샘플 영상"}
[2025-01-15T10:30:02Z] [PHONE_001] [INFO] YouTube 앱 실행 중...
[2025-01-15T10:30:05Z] [PHONE_001] [INFO] 키워드 검색 시작 {"keyword":"여행 브이로그"}
[2025-01-15T10:30:08Z] [PHONE_001] [INFO] 영상 시청 시작 {"title":"샘플 영상"}
[2025-01-15T10:32:30Z] [PHONE_001] [INFO] 영상 시청 완료 {"duration":142}
[2025-01-15T10:32:32Z] [PHONE_001] [INFO] 완료 보고 성공 {"task_id":123}
```

## 🔒 보안 고려사항

1. **API 키 관리**: 프로덕션에서는 API 키 사용 권장
2. **HTTPS 사용**: 프로덕션 환경에서는 반드시 HTTPS
3. **디바이스 ID 관리**: 고유한 ID로 디바이스 추적

## 🚨 문제 해결

### 문제: 서버 연결 실패
```
[ERROR] 서버 연결 실패
```

**해결**:
1. Backend 서버가 실행 중인지 확인
2. 방화벽에서 8000 포트 열기
3. IP 주소가 올바른지 확인 (`ipconfig` 또는 `ifconfig`)

### 문제: YouTube 앱 실행 실패
```
[ERROR] YouTube 앱 실행 실패
```

**해결**:
1. YouTube 앱이 설치되어 있는지 확인
2. 접근성 권한이 AutoX.js에 부여되었는지 확인
3. 폰을 재부팅

### 문제: 작업이 오지 않음
```
[DEBUG] 대기 중인 작업 없음
```

**해결**:
1. Frontend에서 작업을 등록했는지 확인
2. Backend DB에 pending 작업이 있는지 확인:
   ```bash
   curl http://localhost:8000/api/tasks/status
   ```

## 📝 라이선스

MIT License

## 👥 기여자

AIFARM Team
