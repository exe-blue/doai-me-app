# Node Agent (DoAi.Me Runner) — copy to .env and fill. Do not commit .env.
# Same env as desktop-agent: PC_ID/DOAIME_PC_ID, WORKER_TOKEN/DOAIME_WORKER_TOKEN, SERVER_URL.
# Also: PC_CODE, API_BASE_URL, WORKER_API_KEY, NODE_ID, BACKEND_URL, NODE_AGENT_SHARED_SECRET.

# === Required: node identity (per-PC) ===
PC_CODE=PC-01
# (aliases: PC_ID, DOAIME_PC_ID, NODE_ID)

# === Required: backend API base URL ===
API_BASE_URL=https://doai.me
# (aliases: SERVER_URL, BACKEND_URL)

# === Required: worker auth (must match server NODE_SHARED_SECRET or WORKER_SECRET_TOKEN) ===
# Do NOT leave as REPLACE_ME. Same value as server env.
WORKER_API_KEY=
# (aliases: WORKER_TOKEN, DOAIME_WORKER_TOKEN, NODE_AGENT_SHARED_SECRET)

# === Optional: ADB path (default: Windows xiaowei path or 'adb') ===
# ADB_PATH=C:\path\to\adb.exe

# === Optional: Supabase (if used) ===
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=

# === Optional: vendor WebSocket ===
# VENDOR_WS_URL=ws://127.0.0.1:22222
