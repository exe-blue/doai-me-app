# Node Agent (PC running vendor tool + 20 Android devices)
# DoAi.Me MVP Orchestration v1 + Prometheus Workflow
# 각 노드 PC(PC-01~04)에는 로컬에만 .env 둠 (커밋 금지)
# 노드별로 바뀌는 건 NODE_ID뿐; 나머지는 동일

# === 노드 식별 (PC별로 변경) ===
NODE_ID=PC-01

# === 공통 (모든 노드 동일) ===
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

NODE_AGENT_SHARED_SECRET=

# Vendor tool
VENDOR_WS_URL=ws://127.0.0.1:22222

# Backend API
BACKEND_URL=https://your-app.vercel.app

# === Emulator Health Gate (optional) ===
# AVD name for auto-start when device not online (e.g. Pixel_8_API_34)
# EMULATOR_AVD=
# Max wait (ms) for emulator to become ready (default 60000)
# EMULATOR_GATE_WAIT_MS=60000
