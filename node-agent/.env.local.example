# Node Agent 로컬 오버라이드 템플릿
# .env를 먼저 로드하고 .env.local이 덮어씀
# cp .env.local.example .env.local 후 필요한 값만 채우기

# 노드별 변경
NODE_ID=PC-01

# Backend (기본 .env보다 우선)
BACKEND_URL=https://doai.me

# 필요한 경우에만 (비어두면 .env 값 유지)
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
# NODE_AGENT_SHARED_SECRET=
# VENDOR_WS_URL=ws://127.0.0.1:22222
