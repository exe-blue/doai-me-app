# Node Agent — 사전 설정값. 이 파일을 .env.local로 복사하면 입력 없이 사용.
# 설치 시 ProgramData\doai\node-runner\.env.local 로 복사됨.

# 노드 식별 (PC별로 변경 가능)
PC_CODE=PC-01
NODE_ID=PC-01

# 백엔드 주소
API_BASE_URL=https://doai.me
SERVER_URL=https://doai.me
BACKEND_URL=https://doai.me

# 워커 인증 (서버 NODE_SHARED_SECRET / WORKER_SECRET_TOKEN 과 동일하게 설정)
WORKER_API_KEY=
WORKER_TOKEN=
NODE_AGENT_SHARED_SECRET=

# ADB (선택)
# ADB_PATH=C:\path\to\adb.exe

# Supabase (선택)
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=

# Vendor WS (선택)
# VENDOR_WS_URL=ws://127.0.0.1:22222
