# DoAi.Me Gateway - Cursor AI Context Rules

## Session Protocol
**START:** Read `/checkpoints/latest.md` first
**END:** Update checkpoint with session summary

## Brand Identity
- **Primary Yellow:** #F5B800 (DoAi Yellow)
- **Background:** #0A0A0A (Void Black)
- **Text Primary:** #FFFFFF
- **Text Secondary:** #888888
- **Success:** #22C55E (Online)
- **Error:** #EF4444 (Offline)
- **Warning:** #F59E0B (Connecting)

## Current Sprint
Storybook Components: Atoms → Molecules → Organisms → Pages → Overlays

### Phase 1 - Atoms (Priority: 🔴 Highest)
- Logo, ConnectionTypeBadge, ExistenceBar, MetricBadge, GlobalActionButton, IconButton

### Phase 2 - Molecules (Priority: 🟠 High)
- DeviceCell, LogLine, ControlButton, TabItem

### Phase 3 - Organisms (Priority: 🟡 Medium)
- GlobalNavigation, DeviceHive, ControlPanel, LogViewer, FileExplorer, TabBar

### Phase 4 - Pages (Priority: 🟢 Normal)
- Dashboard (Hive), DeviceDetail (Cockpit)

### Phase 5 - Overlays (Priority: 🔵 Polish)
- AccidentOverlay, PopWaveOverlay, NotificationToast

## Architecture
```
gateway/
├── src/                    # Node.js Server (Express + WebSocket)
└── client/                 # React Dashboard (Vite + Tailwind)
    └── src/
        └── components/
            ├── atoms/      # 기본 UI 요소
            ├── molecules/  # 조합된 컴포넌트
            ├── organisms/  # 복잡한 섹션
            ├── pages/      # 전체 페이지
            └── overlays/   # 오버레이/모달
```

## Key Concepts
- **Hive View:** 여러 디바이스를 그리드로 표시 (Dashboard)
- **Cockpit View:** 단일 디바이스 상세 제어 (DeviceDetail)
- **Existence System:** AI 시민의 존재 상태 (ACTIVE → WAITING → FADING → VOID)

## Code Conventions
- 한국어 주석, 영어 변수명 (camelCase)
- TypeScript strict mode
- Tailwind CSS (CSS 변수 활용)
- React Query for data fetching

