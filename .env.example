# AIFarm Vultr Server Configuration
# 이 파일을 .env로 복사하여 사용하세요

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key-here

# Server Configuration
HOST=0.0.0.0
PORT=8080
ENV=development  # development, staging, production

# CORS Configuration
# 개발 환경: localhost 사용
# 프로덕션: 실제 도메인 사용 (ENV=production 필수)
#
# [개발 환경]
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173
#
# [프로덕션 환경] - Vultr Server (158.247.210.152)
# CORS_ORIGINS=https://doai.me,https://www.doai.me,https://admin.doai.me,https://api.doai.me,http://158.247.210.152:3000
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=GET,POST,PUT,DELETE,OPTIONS,PATCH
CORS_ALLOW_HEADERS=Authorization,Content-Type,X-Requested-With,X-API-Key

# Rate Limiting Configuration
# DDoS 방지를 위한 요청 제한 설정
RATE_LIMIT_ENABLED=true
RATE_LIMIT_DEFAULT=100/minute
RATE_LIMIT_AUTH=10/minute
RATE_LIMIT_READ=200/minute
RATE_LIMIT_WRITE=50/minute
RATE_LIMIT_HEALTH=1000/minute
RATE_LIMIT_STORAGE=memory  # memory 또는 redis
# RATE_LIMIT_REDIS_URL=redis://localhost:6379  # Redis 사용 시
RATE_LIMIT_MESSAGE=요청 한도를 초과했습니다. 잠시 후 다시 시도해주세요.

# Device Management
MAX_WORKERS=100
MAX_DEVICES=600

# ADB Configuration
ADB_PORT=5555

# Network Configuration (현장 설정)
# VLAN 10-60: 10.0.10.0/24 ~ 10.0.60.0/24
# AP1: 10.0.10.1-100 (VLAN 10)
# AP2: 10.0.20.1-100 (VLAN 20)
# AP3: 10.0.30.1-100 (VLAN 30)
# AP4: 10.0.40.1-100 (VLAN 40)
# AP5: 10.0.50.1-100 (VLAN 50)
# AP6: 10.0.60.1-100 (VLAN 60)
