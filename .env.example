# Vercel / Backend (API routes)
# Never expose SUPABASE_SERVICE_ROLE_KEY or NODE_AGENT_SHARED_SECRET in frontend
# ⚠️ 키 노출 시 즉시 회전(rotate): docs/Security-and-Setup-Checklist.md

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Node/worker auth: set ONE. All nodes use this shared secret (MVP). Server accepts: NODE_SHARED_SECRET, NODE_AGENT_SHARED_SECRET, WORKER_SECRET_TOKEN, WORKER_TOKEN.
NODE_SHARED_SECRET=
# NODE_AGENT_SHARED_SECRET=
# WORKER_SECRET_TOKEN=
# WORKER_TOKEN=

# Optional: YouTube Data API
YOUTUBE_API_KEY=

# Public site URL (canonical domain)
# NEXT_PUBLIC_SITE_URL=https://doai.me

# Runner update (MVP: semi-auto). Dashboard shows "업데이트 필요 노드 n대" when node runner_version < latest.
LATEST_RUNNER_VERSION=0.1.0
# Optional: direct link to install exe for "다운로드" button (e.g. GitHub Release asset URL)
# RUNNER_DOWNLOAD_URL=https://github.com/owner/repo/releases/download/v0.1.0/doai-node-runner-0.1.0-win-x64.exe
